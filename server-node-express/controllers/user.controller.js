export const UserRouterTest = (req, res) => {
    res.status(200).json({ message: "user router test successfull !" })
}

// adminlogin
    // check if email is present 
    // compare passwords
    // create jwt token
    // send token for auth

// add technician
// app setting 
// create customer

// auth middleware for admin

// new route to register technician
// role: technician

// create technician login

    // email/userid + password
    // jwt

// auth middleware for technician

    // token verified
    // req.user
    // technician _id

    // customer create(created by)/job card (created by)/update job card(updated by)